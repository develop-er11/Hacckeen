import React from 'react';
import '../App.css';
import './HerSection.css';


function HerSection() {
    return ( < div className = 'bg1' >
        <
        div className = 'hero-container' >
        <
        h2 > Blog Diary < /h2> <
        p > Take a glance at Blog < /p>  < /
        div > < /
        div >
    );
}

export default HerSection;